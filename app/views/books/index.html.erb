<div class="container px-5 px-sm-0">
  <%= render 'layouts/errors', obj: @book_new %>
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/info', user: @user %>
      <%= render 'books/newbook', book: @book_new %>
    </div>
      <%= render 'books/index', book: @books %>

  </div>
</div>